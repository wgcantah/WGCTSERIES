% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TSERIES.R
\name{unitroottest}
\alias{unitroottest}
\title{Perform Unit Root Tests using tseries and urca}
\usage{
unitroottest(x)
}
\arguments{
\item{x}{A data frame containing time series data. Only numeric columns are tested.}
}
\value{
A data frame summarizing the test statistics and p-values (or critical values) for each test for each variable.
}
\description{
This function applies several unit root tests on each numeric variable in a data frame.
For each numeric variable, it conducts the Augmented Dickey-Fuller (ADF) test, Phillips-Perron (PP) test,
and KPSS test using functions from the \strong{tseries} package, and the DF-GLS test using \code{ur.ers} from the \strong{urca} package.
}
\details{
For each numeric variable in \code{x}, the function performs:
\itemize{
\item \strong{ADF test}: using \code{tseries::adf.test}, with the test statistic and p-value rounded to two decimals.
\item \strong{PP test}: using \code{tseries::pp.test}, with the test statistic and p-value rounded to two decimals.
\item \strong{KPSS test}: using \code{tseries::kpss.test}, with the test statistic and p-value rounded to two decimals.
\item \strong{DF-GLS test}: using \code{urca::ur.ers} (with a constant model and a maximum lag of 4), with the test statistic rounded to two decimals and the critical values combined into a comma-separated string.
}
The results for each variable are combined into a single data frame.
}
\examples{
\dontrun{
# Example usage:
set.seed(123)
data <- data.frame(
  series1 = rnorm(100),
  series2 = rnorm(100)
)
results <- urootp2(data)
print(results)
}

}
